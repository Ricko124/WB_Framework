const res='WB_UI';window.addEventListener('message',e=>{if(e.data.action==='phone'){phone.style.display='flex';hospital.style.display='none';renderMsgs(e.data.data.messages||[])} if(e.data.action==='hospital'){hospital.style.display='flex';phone.style.display='none'} if(e.data.action==='close'){phone.style.display='none';hospital.style.display='none'}});function post(n,d={}){return fetch(`https://${res}/${n}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(d)})}function renderMsgs(m){messages.innerHTML=m.map(x=>`<div class='card'><b>${x.from_number} → ${x.to_number}</b><p>${x.message}</p></div>`).join('')}function sendMsg(){post('sendMessage',{to:to.value,message:message.value})}function respawn(){post('respawn')}function closeUi(){post('close')}
